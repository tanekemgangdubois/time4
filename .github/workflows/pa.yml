name: jou
on:
  schedule:
    - cron: "*/5 * * * *"
jobs:
  reminder:
    runs-on: ubuntu-latest
    steps:
      - uses: gr2m/set-cron-schedule-action@v2
        with:
          token: ${{ secrtes.TOKEN }}
          cron: |
            */5 * * * *
          workflow: pa.yml
          message: "jou"
